server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stydu5?createDatabaseIfNotExist=true
    username: root
    password: luong123
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      format_sql: true
    show-sql: true